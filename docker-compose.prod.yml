version: '3.8'

services:
  finrisk:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - finrisk_data:/app/data
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
    restart: always

volumes:
  finrisk_data:
